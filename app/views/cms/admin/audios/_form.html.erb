<%= f.text_field :title %>
<%= f.collection_check_boxes :tag_ids, @tags, :id, :name %>
<%= f.file_field :media, required: !@audio.media.attached? %>
<%= render 'com/attachments/list_field', target: f.object.cover, f: f %>
